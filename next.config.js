// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   presets: ["next/babel"],
// };

// module.exports = nextConfig;

/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  /* config options here */
};

module.exports = nextConfig;
