import React from "react";
import AppWrapper from "../components/appWrapper";

const page = () => {
  return (
    <AppWrapper>
      <div>delivery page</div>
    </AppWrapper>
  );
};

export default page;
