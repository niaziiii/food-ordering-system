export interface ICart {
  name: string;
  description: string;
  price: number;
  category: string;
  coverImage: string;
  quantity: number;
}

export interface IMenu {
  name: string;
  description: string;
  price: number;
  category: string;
  coverImage: string;
}
